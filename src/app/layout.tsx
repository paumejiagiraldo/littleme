import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'LittleMe — Personalized Children\'s Storybooks',
  description: 'Turn your child\'s milestones into magical personalized storybooks. Upload a photo, pick a story, download a print-ready PDF for $5.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
